# Singest — Lovable Prompts (5 of 5)

Use these prompts **sequentially** in Lovable. Each one builds on the output of the previous. Copy-paste each one as-is.

Since Lovable runs on **TanStack Start (React 19 + Vite)**, these prompts are written to be framework-agnostic — no Next.js-specific APIs are referenced. Lovable will use its native routing, server functions, and API routes.

---

## Prompt 1 of 5 — Project Foundation, Database, Homepage & News Feed

```
Build a project called "Singest" — a stock analysis and screening dashboard for Indian equity markets.

## TECH STACK
- TypeScript
- Prisma ORM for database access (use Prisma's Data Proxy / Accelerate if needed for the runtime)
- CockroachDB (PostgreSQL-compatible) as the database — connection string is already set as DATABASE_URL secret
- Recharts for charts/graphs (install it)
- Tailwind CSS for styling (with a custom dark theme as the DEFAULT)
- Lucide React for icons
- Google Fonts "Inter" — load via <link> tag in the root layout/HTML head

## DATABASE CONNECTION (Prisma)
Set up Prisma with the following schema. The database already exists and is populated — do NOT run migrations that would drop/alter data. Use `prisma db pull` style introspection. The tables are:

### Table: custom_scan (PRIMARY KEY: isin)
Columns (use these EXACT names — they already exist in the database):
isin VARCHAR(20), sym VARCHAR(20), disp_sym VARCHAR(100), exch VARCHAR(10), inst VARCHAR(20), seg VARCHAR(10), seosym VARCHAR(100), mcap DOUBLE PRECISION, mcapclass VARCHAR(50), pe DOUBLE PRECISION, div_yeild DOUBLE PRECISION, revenue DOUBLE PRECISION, year_1_revenue_growth DOUBLE PRECISION, net_profit_margin DOUBLE PRECISION, yoy_last_qtrly_profit_growth DOUBLE PRECISION, ebidta_margin DOUBLE PRECISION, volume BIGINT, price_perchng_1year DOUBLE PRECISION, price_perchng_3year DOUBLE PRECISION, price_perchng_5year DOUBLE PRECISION, ind_pe DOUBLE PRECISION, pb DOUBLE PRECISION, eps DOUBLE PRECISION, day_sma_50_current_candle DOUBLE PRECISION, day_sma_200_current_candle DOUBLE PRECISION, day_rsi_14_current_candle DOUBLE PRECISION, roce DOUBLE PRECISION, ltp DOUBLE PRECISION, roe DOUBLE PRECISION, rt_away_from_5_year_high DOUBLE PRECISION, rt_away_from_1_month_high DOUBLE PRECISION, high_5yr DOUBLE PRECISION, high_3yr DOUBLE PRECISION, high_1yr DOUBLE PRECISION, high_1wk DOUBLE PRECISION, price_perchng_1mon DOUBLE PRECISION, price_perchng_1week DOUBLE PRECISION, price_perchng_3mon DOUBLE PRECISION, yearly_earning_per_share DOUBLE PRECISION, ocf_growth_on_yr DOUBLE PRECISION, year_1_cagr_eps_growth DOUBLE PRECISION, net_change_in_cash DOUBLE PRECISION, free_cash_flow DOUBLE PRECISION, price_perchng_2week DOUBLE PRECISION, day_bb_upper_sub_bb_lower DOUBLE PRECISION, day_atr_14_current_candle_mul_2 DOUBLE PRECISION, min_5_high_current_candle DOUBLE PRECISION, min_15_high_current_candle DOUBLE PRECISION, min_5_ema_50_current_candle DOUBLE PRECISION, min_15_ema_50_current_candle DOUBLE PRECISION, min_15_sma_100_current_candle DOUBLE PRECISION, open DOUBLE PRECISION, bc_close DOUBLE PRECISION, rmp DOUBLE PRECISION, pledge_benefit DOUBLE PRECISION, lot_size INTEGER, low_1yr DOUBLE PRECISION, multiplier INTEGER, pchange DOUBLE PRECISION, pperchange DOUBLE PRECISION, tick_size DOUBLE PRECISION, fetched_at TIMESTAMPTZ

### Table: live_news (PRIMARY KEY: article_id)
Columns: article_id BIGINT, title TEXT, text TEXT, overall_sentiment VARCHAR(50), category VARCHAR(100), sub_category VARCHAR(100), publish_date TIMESTAMPTZ, stock_name VARCHAR(255), isin_code VARCHAR(50), sm_symbol VARCHAR(100), nse_scrip_code INTEGER, bse_scrip_code INTEGER, seo_symbol VARCHAR(100), display_symbol VARCHAR(100), article_slug VARCHAR(255), article_cat VARCHAR(100), article_subcat VARCHAR(100), metadata_json JSONB, fetched_at TIMESTAMPTZ

### Table: corporate_actions_quarterly_results (PRIMARY KEY: row_hash)
Columns: row_hash VARCHAR(64), isin VARCHAR(20), sym VARCHAR(20), disp_sym VARCHAR(100), exch VARCHAR(10), inst VARCHAR(20), seg VARCHAR(10), seosym VARCHAR(100), ltp DOUBLE PRECISION, volume BIGINT, pchange DOUBLE PRECISION, pperchange DOUBLE PRECISION, act_type VARCHAR(100), ann_date DATE, ann_ltp DOUBLE PRECISION, div_type VARCHAR(50), ex_date DATE, note TEXT, rec_date DATE, rmk TEXT, fetched_at TIMESTAMPTZ

### Tables with IDENTICAL schema to corporate_actions_quarterly_results:
- corporate_actions_bonus
- corporate_actions_dividends
- corporate_actions_splits
- corporate_actions_rights
- corporate_actions_buybacks

## CRITICAL DATA FRESHNESS REQUIREMENT
Every page and API route must fetch fresh data from the database on each request. Do NOT cache database queries. Set `Cache-Control: no-store` on all API responses. Use per-request data fetching in loaders — no caching layer. When a user navigates away and comes back, they must see the latest data if the backend pipeline has run in between.

## DESIGN SYSTEM — DARK THEME (DEFAULT)
- Background: deep charcoal (#0a0a0f) with subtle blue-tinted surfaces (#12121a, #1a1a2e)
- Cards/Panels: glassmorphic dark panels with subtle borders (rgba(255,255,255,0.06)), backdrop-blur
- Primary accent: electric cyan/teal (#00d4aa) for positive values, buttons, active states
- Secondary accent: soft violet (#8b5cf6) for secondary actions and highlights
- Danger/negative: coral red (#ef4444) for negative values/losses
- Text: white (#f8fafc) for primary, muted gray (#94a3b8) for secondary
- All interactive elements (buttons, links, chips) must have smooth hover glow animations (box-shadow transitions, scale transforms). Transitions: 200-300ms ease.
- Page routing transitions should be smooth (use a subtle fade or slide animation)

## HOMEPAGE (Route: /)
Build a homepage with this layout:

### Hero Section
- Large "Singest" logo/text centered at the top with a subtle gradient glow effect (cyan-to-violet)
- Tagline below: "Stock analysis and screening for Indian equity markets."
- A large prominent search bar in the center (dark themed)
  - Search input should query the `custom_scan` table by `disp_sym` or `sym` columns (case-insensitive, partial match using ILIKE)
  - Show a live dropdown of matching results as the user types (debounced at 300ms)
  - Each result shows: disp_sym, sym, mcapclass, ltp with pperchange (green if positive, red if negative)
  - Clicking a result navigates to /stock/$isin page
- Below the search bar: "Or explore:" with 8-10 animated pill/chip buttons showing random popular stocks (fetch top 10 by mcap from custom_scan). Clicking navigates to that stock's page.

### Live News Ticker Section (below hero)
- Section heading: "📰 Latest Market News"
- Fetch the 20 most recent articles from the `live_news` table ordered by `publish_date DESC`
- Display as horizontally scrollable cards (auto-scroll animation optional but nice):
  - Each card: title, stock_name (if available), overall_sentiment as a colored badge (Positive=green, Negative=red, Neutral=gray), relative time (e.g., "2 hours ago")
  - Card background: dark glassmorphic panel
  - On hover: subtle lift + glow effect
  - Cards should NOT be clickable (no detail page for news)

### Market Overview Section (below news)
- Section heading: "📊 Market Pulse"
- Show 3 stat cards side by side:
  1. "Total Stocks Tracked" — count of rows in custom_scan
  2. "Top Gainer Today" — stock with highest pperchange, show name + percentage (green)
  3. "Top Loser Today" — stock with lowest pperchange, show name + percentage (red)
- Cards should have glowing borders that pulse subtly

### Navbar
- Sticky top navbar with:
  - Left: "Singest" brand name (link to /)
  - Center: compact search bar (same search functionality as hero, but smaller)
  - Right: Theme toggle button (sun/moon icon) — just the button UI for now, functionality in Prompt 5
- Navbar should have a glassmorphic blur background

## API ROUTES
Create these server API routes:
1. GET /api/search?q=<query> — Search custom_scan by disp_sym or sym using ILIKE, return top 10 matches with isin, sym, disp_sym, ltp, pperchange, mcapclass
2. GET /api/news?limit=20 — Fetch latest news articles ordered by publish_date DESC
3. GET /api/market-overview — Return total stock count, top gainer, top loser

ALL API routes must set Cache-Control: no-store header and query the database fresh each time.
```

---

## Prompt 2 of 5 — Individual Stock Page: Metrics Dashboard

```
Build the individual stock detail page at route /stock/$isin that displays comprehensive stock data from the existing database.

## DATA FETCHING
- This is a dynamic page. Fetch the stock data from the `custom_scan` table by the `isin` URL parameter on EVERY request (no caching).
- Also fetch related news from `live_news` where `isin_code` matches the stock's `isin`.
- If the ISIN is not found, show a styled 404 page saying "Stock not found" with a link back to home.
- Create API route: GET /api/stock/$isin — returns the full custom_scan row + related news for that ISIN. Set Cache-Control: no-store.

## PAGE LAYOUT
The page should have a fixed navbar at the top (same as homepage with compact search bar + theme toggle). Below that:

### Stock Header Section
- Left side:
  - Stock name: `disp_sym` in large bold text (e.g., "HBL Engineering Ltd")
  - Below it: `sym` in muted text + Exchange badge showing `exch` (e.g., "NSE") + Market Cap class badge showing `mcapclass` (e.g., "Large Cap" in blue, "Mid Cap" in violet, "Small Cap" in teal)
- Right side:
  - Current price: `ltp` formatted as ₹XX,XXX.XX in large text
  - Below price: `pperchange` as percentage with colored arrow icon (▲ green if positive, ▼ red if negative) and the absolute change `pchange`
  - Small muted text: "Last updated: " + `fetched_at` formatted as relative time

### Key Metrics Grid (KPI Cards)
Display the following metrics in a responsive 3-column grid (2 columns on tablet, 1 on mobile) of glassmorphic cards. Each card has:
- A label in muted text
- The value in large bold text
- An appropriate icon from Lucide

Cards to show (use EXACT database column names when querying):
| Label | DB Column | Format |
|---|---|---|
| Market Cap | mcap | ₹XX,XXX Cr. (divide by 10000000 to get crores) |
| P/E Ratio | pe | XX.XX |
| P/B Ratio | pb | XX.XX |
| EPS | eps | ₹XX.XX |
| Industry P/E | ind_pe | XX.XX |
| Dividend Yield | div_yeild | XX.XX% |
| ROCE | roce | XX.XX% |
| ROE | roe | XX.XX% |
| EBITDA Margin | ebidta_margin | XX.XX% |
| Net Profit Margin | net_profit_margin | XX.XX% |
| Revenue | revenue | ₹XX,XXX Cr. |
| Revenue Growth (1Y) | year_1_revenue_growth | XX.XX% |
| QoQ Profit Growth | yoy_last_qtrly_profit_growth | XX.XX% |
| EPS Growth (1Y CAGR) | year_1_cagr_eps_growth | XX.XX% |
| Free Cash Flow | free_cash_flow | ₹XX,XXX Cr. |
| OCF Growth | ocf_growth_on_yr | XX.XX% |

For percentage values, color them: green if positive, red if negative, gray if zero/null. If a value is null, display "—" in muted gray.

### Price Performance Section
- Section heading: "📈 Price Performance"
- Create a horizontal bar chart (using Recharts BarChart) showing price change percentages across timeframes:
  - Bars for: 1W (price_perchng_1week), 2W (price_perchng_2week), 1M (price_perchng_1mon), 3M (price_perchng_3mon), 1Y (price_perchng_1year), 3Y (price_perchng_3year), 5Y (price_perchng_5year)
  - Bar color: green for positive, red for negative
  - Show percentage value at the end of each bar
  - Dark themed chart with grid lines in rgba(255,255,255,0.05)
  - Chart background: transparent, sits inside a glassmorphic card

### Price Highlights Grid
- Section heading: "🎯 Price Highlights"
- Display in a 2x4 grid of small stat cards:
  | Label | Column | Format |
  |---|---|---|
  | Open | open | ₹XX.XX |
  | Prev Close | bc_close | ₹XX.XX |
  | 1-Week High | high_1wk | ₹XX.XX |
  | 1-Year High | high_1yr | ₹XX.XX |
  | 1-Year Low | low_1yr | ₹XX.XX |
  | 3-Year High | high_3yr | ₹XX.XX |
  | 5-Year High | high_5yr | ₹XX.XX |
  | Away from 5Y High | rt_away_from_5_year_high | XX.XX% |

### Technical Indicators Section
- Section heading: "⚙️ Technical Indicators"
- Display these in a row of indicator cards:
  1. **RSI (14)**: `day_rsi_14_current_candle` — show as a semicircular gauge chart (Recharts PieChart or custom SVG). Color zones: 0-30 = oversold (green), 30-70 = neutral (gray), 70-100 = overbought (red). Show the label "Oversold"/"Neutral"/"Overbought" below.
  2. **SMA 50**: `day_sma_50_current_candle` — show value with comparison to LTP. If LTP > SMA50, show "Price above SMA50" in green. Otherwise red.
  3. **SMA 200**: `day_sma_200_current_candle` — same treatment as SMA50.
  4. **Bollinger Width**: `day_bb_upper_sub_bb_lower` — show value.
  5. **ATR (14)**: `day_atr_14_current_candle_mul_2` — show value (this is ATR×2).

### Stock News Section (at bottom)
- Section heading: "📰 Latest News for [disp_sym]"
- Fetch news where `isin_code` = stock's `isin` from live_news table, ordered by publish_date DESC, limit 10.
- Display as a vertical list of news cards with title, sentiment badge (colored), relative time.
- If no news found, show a muted message: "No recent news available for this stock."

## ANIMATIONS
- All cards should have a staggered fade-in animation when the page loads (use CSS @keyframes or framer-motion)
- Hover on any card: subtle scale(1.02) + glow shadow transition
- The performance bar chart should animate bars growing from zero on page load
```

---

## Prompt 3 of 5 — Corporate Actions Section (Tabs)

```
Add a Corporate Actions section to the /stock/$isin page, placed between the Technical Indicators section and the Stock News section.

## DATA FETCHING
Create these API routes (all must have Cache-Control: no-store, no caching):
1. GET /api/stock/$isin/dividends — Query `corporate_actions_dividends` WHERE isin = $isin, ORDER BY ex_date DESC
2. GET /api/stock/$isin/bonus — Query `corporate_actions_bonus` WHERE isin = $isin, ORDER BY ex_date DESC
3. GET /api/stock/$isin/splits — Query `corporate_actions_splits` WHERE isin = $isin, ORDER BY ex_date DESC
4. GET /api/stock/$isin/rights — Query `corporate_actions_rights` WHERE isin = $isin, ORDER BY ex_date DESC
5. GET /api/stock/$isin/buybacks — Query `corporate_actions_buybacks` WHERE isin = $isin, ORDER BY ex_date DESC
6. GET /api/stock/$isin/quarterly-results — Query `corporate_actions_quarterly_results` WHERE isin = $isin, ORDER BY ex_date DESC

IMPORTANT: All 6 tables share the EXACT same schema with these columns:
row_hash, isin, sym, disp_sym, exch, inst, seg, seosym, ltp, volume, pchange, pperchange, act_type, ann_date, ann_ltp, div_type, ex_date, note, rec_date, rmk, fetched_at

## UI LAYOUT

### Section Heading
"📋 Corporate Actions" in the same style as other section headings on the page.

### Tab Navigation
Create a horizontal tab bar with 6 tabs. Tabs should have:
- Smooth underline indicator that slides to the active tab
- Glow effect on active tab text (primary cyan color)
- Tab labels: "Dividends", "Bonus", "Splits", "Rights", "Buybacks", "Quarterly Results"
- Each tab shows a count badge (number of records) next to the label — fetch counts for all 6 on page load
- If a tab has 0 records, show the badge in muted gray but keep the tab accessible

### Tab Content — Data Tables
When a tab is selected, display its data in a styled dark-themed table inside a glassmorphic card. Use client-side tab switching (no page reload). Fetch data for each tab lazily (only when first clicked).

Table columns to display for each tab:

**Dividends Tab:**
| Column Header | DB Column | Format |
|---|---|---|
| Ex-Date | ex_date | DD MMM YYYY |
| Announcement Date | ann_date | DD MMM YYYY |
| Dividend Type | div_type | Text |
| LTP at Announcement | ann_ltp | ₹XX.XX |
| Record Date | rec_date | DD MMM YYYY |
| Remarks | rmk | Text (truncate to 50 chars with tooltip for full text) |

**Bonus Tab:**
| Column Header | DB Column | Format |
|---|---|---|
| Ex-Date | ex_date | DD MMM YYYY |
| Announcement Date | ann_date | DD MMM YYYY |
| LTP at Announcement | ann_ltp | ₹XX.XX |
| Record Date | rec_date | DD MMM YYYY |
| Details | note | Text |
| Remarks | rmk | Text |

**Splits Tab:**
| Column Header | DB Column | Format |
|---|---|---|
| Ex-Date | ex_date | DD MMM YYYY |
| Announcement Date | ann_date | DD MMM YYYY |
| LTP at Announcement | ann_ltp | ₹XX.XX |
| Record Date | rec_date | DD MMM YYYY |
| Split Details | note | Text |
| Remarks | rmk | Text |

**Rights Tab:**
| Column Header | DB Column | Format |
|---|---|---|
| Ex-Date | ex_date | DD MMM YYYY |
| Announcement Date | ann_date | DD MMM YYYY |
| LTP at Announcement | ann_ltp | ₹XX.XX |
| Record Date | rec_date | DD MMM YYYY |
| Details | note | Text |
| Remarks | rmk | Text |

**Buybacks Tab:**
| Column Header | DB Column | Format |
|---|---|---|
| Ex-Date | ex_date | DD MMM YYYY |
| Announcement Date | ann_date | DD MMM YYYY |
| LTP at Announcement | ann_ltp | ₹XX.XX |
| Record Date | rec_date | DD MMM YYYY |
| Details | note | Text |
| Remarks | rmk | Text |

**Quarterly Results Tab:**
| Column Header | DB Column | Format |
|---|---|---|
| Quarter (Ex-Date) | ex_date | DD MMM YYYY |
| Announcement Date | ann_date | DD MMM YYYY |
| LTP at Announcement | ann_ltp | ₹XX.XX |
| Record Date | rec_date | DD MMM YYYY |
| Details | note | Text |
| Remarks | rmk | Text |

### Table Styling
- Dark themed table with alternating row backgrounds (#12121a and #1a1a2e)
- Header row: slightly brighter background with bottom border glow
- Columns should be sortable (click header to toggle ASC/DESC) — add subtle sort arrow icons
- Dates that are in the future should have a small "Upcoming" badge next to them in teal
- Hover on any row: subtle highlight effect (brighten the row background)
- Null/empty values display as "—"
- If the table is empty (no records), show a centered message: "No [action type] records found for this stock." with a muted icon

### Dividend Visualization (Extra — Dividends Tab Only)
Below the dividends table (only visible when Dividends tab is active), add a horizontal bar chart (Recharts) showing dividend amounts over time:
- X-axis: ex_date (formatted as MMM YYYY)
- Y-axis: ann_ltp (as a proxy indicator — the LTP at announcement time)
- Bar color: gradient from teal to cyan
- Only show this chart if there are 3 or more dividend records. Otherwise skip it.
- Title: "Dividend Announcement Timeline"

### Animation
- Tab content should transition with a subtle fade-in when switching tabs
- Table rows should stagger in with a cascading fade animation
```

---

## Prompt 4 of 5 — Peer Comparison & Stock Screener Page

```
Add two major features: a Peer Comparison section on the stock page, and a new dedicated Stock Screener page.

## PART A: PEER COMPARISON (on /stock/$isin page)

Place this section between the "Price Performance" section and the "Price Highlights" section.

### Data Fetching
Create API route: GET /api/stock/$isin/peers (Cache-Control: no-store)
- First, look up the stock's `mcapclass` from `custom_scan` using the ISIN
- Then query `custom_scan` for all stocks WHERE `mcapclass` = same value, ORDER BY `mcap` DESC, LIMIT 10
- Always include the current stock in the results (even if it's not in the top 10 by mcap — prepend it or ensure it's included)
- Return: isin, sym, disp_sym, ltp, pperchange, mcap, pe, div_yeild, roce, roe, eps, pb, net_profit_margin, volume

### UI Layout
- Section heading: "👥 Peer Comparison" with a subtitle "Compared with other [mcapclass] stocks"
- Display as a styled horizontal-scroll table (for mobile) inside a glassmorphic card
- The current stock's row should be highlighted with a subtle cyan left-border and slightly brighter background

Table columns:
| Column Header | DB Column | Format |
|---|---|---|
| # | (row index) | Number |
| Name | disp_sym | Text (clickable → navigates to /stock/$isin) |
| CMP (₹) | ltp | ₹XX,XXX.XX |
| Change % | pperchange | XX.XX% (green/red colored) |
| Market Cap (Cr.) | mcap | ₹XX,XXX (divide by 10000000) |
| P/E | pe | XX.XX |
| P/B | pb | XX.XX |
| Div Yield % | div_yeild | XX.XX% |
| ROCE % | roce | XX.XX% |
| ROE % | roe | XX.XX% |
| NPM % | net_profit_margin | XX.XX% |
| EPS | eps | ₹XX.XX |

- Table styling: same dark theme as corporate actions tables
- Sortable columns (click to sort ASC/DESC)
- Hover row highlight
- Name column links should have a subtle cyan hover color

---

## PART B: STOCK SCREENER PAGE (Route: /screener)

### Navigation
Add "Screener" as a link in the navbar (between the brand name and the search bar).

### Page Layout

#### Page Header
- Title: "Stock Screener" in large bold text
- Subtitle: "Filter and analyze [total_count] NSE-listed stocks" (fetch actual count from custom_scan)

#### Filter Bar
Create a horizontal filter bar at the top with these filter controls:
1. **Market Cap Class** — Dropdown multi-select with options fetched from DISTINCT `mcapclass` values in custom_scan (e.g., "Large Cap", "Mid Cap", "Small Cap", "Micro Cap")
2. **P/E Range** — Two number inputs (min, max)
3. **ROCE Min** — Single number input
4. **ROE Min** — Single number input
5. **Dividend Yield Min** — Single number input

- Add a "Apply Filters" button (primary cyan with glow) and a "Reset" button (muted outline)
- Filters should be sent as query parameters to the API

#### API Route
GET /api/screener?mcapclass=Large+Cap,Mid+Cap&pe_min=0&pe_max=50&roce_min=15&roe_min=10&div_yield_min=1&sort=mcap&order=desc&page=1&limit=50
- Build the WHERE clause dynamically based on provided query params
- Support sorting by any numeric column
- Support pagination (page + limit)
- Return total count for pagination UI
- Cache-Control: no-store, no caching

#### Results Table
Display filtered stocks in a full-width dark-themed table:

| Column Header | DB Column | Format |
|---|---|---|
| # | (row index) | Number |
| Name | disp_sym | Text (link to /stock/$isin) |
| Symbol | sym | Text |
| CMP (₹) | ltp | ₹XX,XXX.XX |
| Change % | pperchange | XX.XX% (green/red) |
| Market Cap (Cr.) | mcap | ₹XX,XXX |
| Cap Class | mcapclass | Badge (colored by class) |
| P/E | pe | XX.XX |
| P/B | pb | XX.XX |
| Div Yield % | div_yeild | XX.XX% |
| ROCE % | roce | XX.XX% |
| ROE % | roe | XX.XX% |
| EPS | eps | ₹XX.XX |
| NPM % | net_profit_margin | XX.XX% |
| 1Y Return % | price_perchng_1year | XX.XX% (green/red) |
| Volume | volume | XX,XXX,XXX |

- All columns sortable (click header)
- Current sort column should have a visible arrow indicator
- Pagination at the bottom: "Showing 1-50 of 2,929 stocks" with Previous/Next buttons (glow effect on hover)
- Table rows: alternating dark backgrounds, hover highlight
- Name column: clickable link to /stock/$isin with cyan hover color
- mcapclass badges: Large Cap = blue, Mid Cap = violet, Small Cap = teal, Micro Cap = amber

#### Empty/Loading States
- While loading: show a skeleton table (animated pulsing rows)
- If no results match filters: "No stocks match your criteria. Try adjusting your filters."

### Animation
- Filter bar: smooth slide-down animation on page load
- Table rows: cascading fade-in
- Pagination transitions: fade content when loading next page
```

---

## Prompt 5 of 5 — Theme Toggle, Polish, Performance & Deployment

```
This is the final polish pass. Add theme toggling, smooth animations, loading states, error handling, responsive design, and SEO.

## THEME TOGGLE (Dark/Light)
Implement a working dark/light theme toggle:
- Use CSS custom properties (CSS variables) on :root and a [data-theme="light"] selector, or use a React context + Tailwind dark class approach — whatever works best in this framework.
- Dark theme is the DEFAULT (as already built)
- Light theme colors:
  - Background: #f0f4f8 (soft blue-gray)
  - Cards/Panels: white (#ffffff) with subtle shadows (0 4px 24px rgba(0,0,0,0.06))
  - Primary accent: #0d9488 (teal-600) for positive values, buttons
  - Secondary accent: #7c3aed (violet-600)
  - Danger: #dc2626 (red-600)
  - Text: #0f172a (primary), #64748b (secondary)
  - Table rows: alternating #ffffff and #f8fafc
  - Navbar: white with bottom border shadow
- The theme toggle button in the navbar (sun/moon icon) should now work — clicking it toggles between dark and light. Use a smooth rotation + fade animation on the icon transition.
- Persist theme choice in localStorage. On first visit, default to dark.
- Ensure ALL components respect the theme: cards, tables, charts, badges, search dropdowns, filter inputs, buttons, tab bars, news cards, gauge charts, bar charts. Nothing should look broken in light mode.

## SMOOTH PAGE TRANSITIONS
- Add a subtle page transition animation when navigating between routes:
  - Fade in (opacity 0 → 1 over 300ms) + slight slide up (translateY 10px → 0)
  - Use a layout-level wrapper component for this
- The navbar search dropdown should have a smooth slide-down animation when results appear and slide-up when closing

## LOADING STATES
Add proper loading UIs for every data-fetching component:
- **Homepage**: Skeleton placeholders for news cards (3 pulsing card shapes), market overview stat cards (3 pulsing rectangles), and stock chips
- **Stock Page**: Full skeleton layout matching the real page structure — skeleton for header, KPI grid (16 pulsing cards), performance chart area, price highlights grid, technical indicators, corporate actions tab area
- **Screener Page**: Skeleton filter bar + skeleton table rows (10 rows of pulsing cells)
- **Search Dropdown**: Show a small spinner while results are loading
- All skeletons should use the dark theme background colors with a subtle shimmer/pulse animation

## ERROR HANDLING
- Wrap each major section (KPI grid, performance chart, corporate actions, peer comparison, news, screener table) in an error boundary
- If a section fails to load data, show a graceful error card: "Unable to load [section name]. Please refresh." with a retry button
- Never show raw error messages or stack traces to the user
- If the database connection fails entirely, show a full-page error: "Singest is currently unable to connect to the data source. Please try again later." with the Singest branding

## RESPONSIVE DESIGN
Ensure everything works beautifully on:
- **Desktop** (1200px+): Full layouts as designed
- **Tablet** (768px-1199px):
  - KPI grid: 2 columns
  - Peer comparison table: horizontal scroll
  - Screener table: horizontal scroll
  - News cards: 2 per row
- **Mobile** (< 768px):
  - KPI grid: 1 column
  - All tables: horizontal scroll with a "Scroll →" hint
  - Navbar: collapse search bar into a search icon that expands on tap
  - Corporate actions tabs: horizontally scrollable tab bar
  - Filter bar on screener: stack vertically
  - News cards: 1 per row, full width

## SEO
- Add proper metadata to each page using standard HTML <title> and <meta> tags:
  - Homepage: title "Singest — Stock Analysis & Screening for Indian Markets", description about the platform
  - Stock page: title "[disp_sym] — Stock Analysis | Singest", description with key metrics
  - Screener page: title "Stock Screener — Filter NSE Stocks | Singest"
- Use semantic HTML: proper heading hierarchy (single h1 per page), nav, main, section, article elements
- Add Open Graph meta tags for social sharing

## PERFORMANCE
- Lazy-load heavy chart components (Recharts) using React.lazy() with loading skeletons as fallback
- Ensure Prisma client is instantiated as a singleton (prevent connection pool exhaustion in serverless)

## FINAL CHECKLIST (verify all of these)
- [ ] Dark theme is default, light theme works perfectly
- [ ] Every page fetches fresh data on each load (no stale data)
- [ ] Search works on homepage and navbar (both)
- [ ] Stock page shows all metrics, charts, corporate actions, peers, news
- [ ] Screener page filters, sorts, and paginates correctly
- [ ] All table column names match the EXACT database column names (no mismatches)
- [ ] Null values display as "—", not "null" or "undefined"
- [ ] Green/red coloring is consistent for positive/negative values everywhere
- [ ] All animations are smooth (no janky transitions)
- [ ] Mobile responsive — nothing overflows or breaks
- [ ] No console errors in browser
- [ ] Build succeeds with no TypeScript errors
```

---

> [!TIP]
> **Usage Instructions**: Paste each prompt into Lovable one at a time, in order (1 → 2 → 3 → 4 → 5). Wait for each one to complete before pasting the next. If Lovable asks clarifying questions, answer them based on the context above. Make sure your `DATABASE_URL` secret is added in Lovable's project settings before starting Prompt 1.

> [!NOTE]
> These prompts are framework-agnostic — they avoid referencing Next.js-specific APIs (App Router, next/font, next/dynamic, next-themes, next/image). Lovable will use its native TanStack Start equivalents (file routes, server functions, loaders, etc.) and the end result will be functionally identical.
